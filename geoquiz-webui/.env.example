# Local Vite -> API (default when unset is also http://localhost:8080)
VITE_API_BASE_URL=http://localhost:8080

# Docker web image uses VITE_API_BASE_URL= (empty) so the browser calls same-origin /api via nginx.
